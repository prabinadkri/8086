TITLE SUM OF EVENNUM
.MODEL SMALL
.STACK 64
.DATA
.CODE
MAIN PROC FAR
	MOV AX,@DATA
	MOV DS,AX
	
	MOV CX,100
	MOV AX,00
	MOV BX,00
LOO:ADD BX,02
	ADD AX,BX
	LOOP LOO
	
	;COVNERTING TO DECIMAL AND DISPLAYING
	MOV CX,00
	MOV BX,10
	NEX:MOV DX,00
	DIV BX
	ADD DX,30H
	PUSH DX
	INC CX
	CMP AX,00
	JA NEX
	
	MOV AH,02H
DISP:POP DX
	INT 21H
	LOOP DISP
	MOV AX,4C00H
	INT 21H
MAIN ENDP
END MAIN
	
	